//to calculate the number of days between the given set of two dates without any functions //
#include <stdio.h>
#include <conio.h>

int main(){
   int a=0,b=0,c=0,a1=0,b1=0,c1=0;
   printf("Enter the start date in format dd-mm-yyyy\n");
   scanf("%d",&a);
   scanf("%d",&b);
   scanf("%d",&c);
   printf("Enter the end date in format dd-mm-yyyy\n");
   for ()

}